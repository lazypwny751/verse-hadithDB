CREATE TABLE verse (
    source TEXT NOT NULL,
    context TEXT NOT NULL,
    source_url TEXT NOT NULL
);

CREATE TABLE hadith (
    source TEXT NOT NULL,
    context TEXT NOT NULL,
    source_url TEXT NOT NULL
);